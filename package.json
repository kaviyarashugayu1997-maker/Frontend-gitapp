{
	  "name": "frontend-devops-dashboard",
	    "version": "1.0.0",
	      "main": "server.js",
	        "scripts": {
			    "start": "node server.js",
			        "build": "mkdir -p dist && cp *.html dist/ 2>/dev/null || true && cp -r public/* dist/ 2>/dev/null || true"
				  },
				    "dependencies": {
					        "express": "^4.18.2"
						  }
}
